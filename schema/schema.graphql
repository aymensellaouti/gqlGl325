type Query {
  hello: String!
  users: [User]
  workers(isWorking:Boolean): [User] 
  roles: [Role!]
}

type User {
  id: ID!
  name: String
  age: Int
  isWorking: Boolean
  roles: [Role]
}

type Role {
  id: ID!
  designation: String,
  methods: [String]
}